// Generated migrations live in `@routine-keeper/core/drizzle/` (next to the schema).
// The `.sql` files are inlined into the bundle by babel-plugin-inline-import
// (see babel.config.js) + the `sql` sourceExt (see metro.config.js).
import migrations from '@routine-keeper/core/drizzle/migrations';

export { migrations };
